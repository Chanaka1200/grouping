global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      # UPDATE: Replace with Choreo internal URL for otel-collector metrics endpoint
      # Format: <project>--<component>--<endpoint>.<namespace>:8889
      - targets: ['otel-collector:8889']
